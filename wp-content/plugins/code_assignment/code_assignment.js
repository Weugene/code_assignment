// // Assume you have the URL-encoded variable stored in a JavaScript variable called encodedVariable
// jQuery(document).ready(function($) {
  
//     // var codeVar = `{{sample_code|raw}}`;
//     // var htmlElement = document.createElement('div');
//     // htmlElement.innerHTML = codeVar;
//     // var textContent = htmlElement.textContent;
  
//     // console.log("codeVar");
//     // console.log(textContent);
//     // var encodedVariable = encodeURIComponent(textContent);
  
//     // // Get the reference to the iframe element
//     // var iframe = document.getElementById("{{uc_serial}}");
  
//     // // Update the src attribute of the iframe
//     // iframe.src = "https://trinket.io/embed/python3?listen=true#code=" + encodedVariable;
//     // // Set interval for sending requests every 10 seconds
//     // // setInterval(sendRequest, 2000);
//   });